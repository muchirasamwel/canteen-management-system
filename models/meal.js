class Meal {
  constructor (id, categoryIds, title, imageUrl, ingredients, price) {
    this.id = id
    this.categoryIds = categoryIds
    this.title = title
    this.imageUrl = imageUrl
    this.ingredients = ingredients
    this.price = price
  }
}

export default Meal
